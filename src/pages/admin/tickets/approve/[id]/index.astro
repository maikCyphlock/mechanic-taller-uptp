---
import Layout from "@/layout/layout.astro";
import TicketApprove from "@/components/ticket/TicketApprove.svelte"

const ticketId = Astro.params.id

const AdminId = Astro.locals.user?.id

---

<Layout>
    <TicketApprove idTicket={ticketId} adminId={AdminId} client:load />
</Layout>