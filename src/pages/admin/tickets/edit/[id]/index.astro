---
import Layout from "@/layout/layout.astro";
import TicketCreate from "@/components/ticket/TicketEdit.svelte"

const ticketId = Astro.params.id

---

<Layout>
    <TicketCreate idTicket={ticketId} client:load />
</Layout>