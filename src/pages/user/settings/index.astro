---
import Settings from './_settings.svelte'
import Layout from '@/layout/layout.astro'

const userId = Astro.locals.user?.id || null;
console.log('User ID:', userId);
console.log()
---
<Layout>

    <Settings userId={userId} Context={Astro.request} client:load />
</Layout>